using System.Web.Mvc;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using PeoplePro.Controllers;
using System.Threading.Tasks;

namespace PeoplePro.Models
{
    [TestClass]
    public class BuildingsControllerTest
    {
       /* private readonly PeopleProContext _context;

        public BuildingsControllerTest(PeopleProContext context)
        {
            _context = context;
        }
        [TestMethod]
        public void Index()
        {
            Building b = new Building()
            {
                Name = "TestName",
                Address = "1234",
                Id = 100

            };
            BuildingsController controller = new BuildingsController(_context);
            var result = controller.Create(b).Result;
            var Building = (Building)result.m


        }
        
    */
    }
}
