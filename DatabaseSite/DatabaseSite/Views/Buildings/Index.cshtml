@model IEnumerable<DatabaseSite.Models.Building>

@{
    ViewBag.Title = "Buildings";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model){
        @Html.Partial("~/Views/Shared/Partials/BuildingPartial.cshtml", item)
    }
</table>
